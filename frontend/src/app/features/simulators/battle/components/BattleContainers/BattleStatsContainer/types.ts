export interface BattleStatsContainerType {
  toggleTraits: boolean;
  setToggleTraits: React.Dispatch<React.SetStateAction<boolean>>;

  toggleBattleEndStats: boolean;
  setToggleBattleEndStats: React.Dispatch<React.SetStateAction<boolean>>;
}
